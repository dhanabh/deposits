<% form_for @fdr  do |f| %>
<%=  collection_select(:holder, :id, @h , :id,:full_name, {:prompt => true})%>
<%= collection_select(:fdrs, :id, Holder.all, :id, :full_name, :prompt => true)%>



<% end %>

